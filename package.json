{
  "name": "checquery",
  "private": true,
  "workspaces": [
    "client",
    "shared",
    "server"
  ],
  "trustedDependencies": [
    "@tailwindcss/oxide"
  ],
  "scripts": {
    "build": "bun run build.ts",
    "start": "bun run launcher.ts"
  }
}
